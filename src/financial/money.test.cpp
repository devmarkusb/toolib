#include "toolib/financial/money.h"
#include "gtest/gtest.h"
#include <iostream>

TEST(CurrencyTest, test)
{
    // doesn't work yet :( Why?!
    //    too::fin::Currency curr(std::locale(""));
    //    std::cout << "locale name: " << curr.getLocaleConstrName() << "\n";
    //    std::cout << "currency string: " << curr.getString() << "\n";
    //    std::cout << "currency symbol: " << curr.getSymbol() << "\n\n";
    //    too::fin::Currency c2(std::locale("en_US.utf8"));
    //    std::cout << "locale name: " << c2.getLocaleConstrName() << "\n";
    //    std::cout << "currency string: " << c2.getString() << "\n";
    //    std::cout << "currency symbol: " << c2.getSymbol() << "\n";
}
