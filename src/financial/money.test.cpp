#include "toolib/financial/money.h"
#include "gtest/gtest.h"
#include <iostream>

TEST(CurrencyTest, test) {
    // doesn't work yet :( Why?!
    //    too::fin::Currency curr(std::locale(""));
    //    std::cout << "locale name: " << curr.get_locale_constr_name() << "\n";
    //    std::cout << "currency string: " << curr.get_string() << "\n";
    //    std::cout << "currency symbol: " << curr.get_symbol() << "\n\n";
    //    too::fin::Currency c2(std::locale("en_US.utf8"));
    //    std::cout << "locale name: " << c2.get_locale_constr_name() << "\n";
    //    std::cout << "currency string: " << c2.get_string() << "\n";
    //    std::cout << "currency symbol: " << c2.get_symbol() << "\n";
}
